/** @jsx jsx */
import { jsx } from "theme-ui";

const Illustrations = ({children}) => (
    <div style={{"float": "right", "text-align": "center", "max-width": "38%", "padding-left": "10px", "margin-top": "-25px"}}>
        {children}
    </div>
);

export default Illustrations;
